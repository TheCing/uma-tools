// Setup file for tests that need build constants
// Load this before other imports using: npx ts-node -r ./test/v1-v2-parity/setup.ts

(global as any).CC_GLOBAL = true;
(global as any).CC_DEBUG = false;
